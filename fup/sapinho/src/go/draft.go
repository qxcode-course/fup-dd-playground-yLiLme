package main
import "fmt"
func main() {
    var p, s, e, qts, qtd int

    fmt.Scan(&p, &s, &e)

    qts=s
    for i:=0; i<=p; i++ {
        fmt.Printf("%d %d\n", qtd, qts)
        
        
    }

}
