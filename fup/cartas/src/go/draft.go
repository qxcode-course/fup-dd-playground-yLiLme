package main
import "fmt"
func main() {
    var n, nums int
    fmt.Scan(&n)
    var cartas[]int = make([]int, n)

    for i := 0; i < n; i++ {
        fmt.Scan(&nums)
        cartas[i]=nums
    }
    for i:=0; i<n; i++{
        
        
    }
    
}