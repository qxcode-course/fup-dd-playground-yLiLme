package main
import "fmt"
func main() {
    var figualbu, figubaru, numfigus int64
    fmt.Scan(&figualbu)
    var vet[]int=make([]int, figualbu)
    fmt.Scan(&figubaru)
    var vetfigubaru[]int=make([]int, figubaru)

    for i:=0; i<int(figubaru); i++ {
        fmt.Scan(&numfigus)
        vetfigubaru[i]=int(numfigus)
    }
    for  i:= range figubaru {
        
    }
}
