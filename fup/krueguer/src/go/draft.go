package main

import (
	"bufio"
	"fmt"
	"go/scanner"
	"os"
)

func main() {
    var n int
    fmt.Scan(&n)

    var vet[]string=make([]string, n)

    scanner:= bufio.NewScanner(os.Stdin)
    scanner.Scan()

    sequelAtual:=""
    maiorSequel:=""

    for i:=0; i<len(); i++ {
        scanner.Scan()
        frase:=scanner.Text()

        
    } 


}